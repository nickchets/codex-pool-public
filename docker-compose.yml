services:
  codex-pool:
    build: .
    ports:
      - "127.0.0.1:8989:8989"
    volumes:
      - ./pool:/app/pool:rw
      - ./config.toml:/app/config.toml:ro
    environment:
      CODEX_POOL_LISTEN_ADDR: "0.0.0.0:8989"
